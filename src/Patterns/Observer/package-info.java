package Patterns.Observer;
/*
 * Patterns.Observer Pattern: defines a one-to-many dependency between objects so that when one object changes state,
 * all its dependents are notified a d updated automatically
 *
 * Create subject interface with add, remove, notify observer methods
 * Create observer interface with update function
 * Two types: push or pull
 * */